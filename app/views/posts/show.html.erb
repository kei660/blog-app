<div class="post-detail">
  <h1><%= @post.title %></h1>
  <p><%= @post.content %></p>
  <p class="post-meta">投稿者: <%= @post.user.email %> | 投稿日: <%= @post.created_at.strftime("%Y年%m月%d日 %H:%M") %></p>
  <%= link_to "戻る", root_path, class: "button" %>
</div>
